name: smart_complaint_system
description: A simplified Flutter-based Smart Complaint Management System
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.0.0
  provider: ^6.0.5
  flutter_local_notifications: ^17.0.0
  excel: ^2.0.0-nullsafety-3
  file_picker: ^8.0.0
  url_launcher: ^6.1.10
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/