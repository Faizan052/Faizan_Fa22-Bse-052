name: complaint_guru
description: "A Flutter-based Smart Complaint Management System."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.0.0
  provider: ^6.0.5
  flutter_local_notifications: ^17.0.0
  excel: ^2.0.0-nullsafety-3
  file_picker: ^8.0.0
  animate_do: ^3.0.2
  http: ^1.1.0
  shared_preferences: ^2.2.0
  cupertino_icons: ^1.0.8
  url_launcher: ^6.1.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
